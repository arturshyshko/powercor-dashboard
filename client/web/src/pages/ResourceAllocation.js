import React from 'react'
import siteTemplate from '@components/template'
import { ResourceAllocationTable } from '@components/tables'


const ResourceAllocation = (props) => (
    <ResourceAllocationTable />
)

export default siteTemplate(ResourceAllocation)
